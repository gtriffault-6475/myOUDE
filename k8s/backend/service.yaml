apiVersion: v1
kind: Service
metadata:
  name: oude-backend-svc
  namespace: oude
spec:
  selector:
    app: oude-backend
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP   # Interne uniquement — le front proxy via Nginx
